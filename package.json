{
  "name": "eluno",
  "version": "1.0.0",
  "private": true,
  "description": "El Uno — The Law of One in a philosophical lens",
  "license": "AGPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/chuchurex/eluno.git"
  },
  "dependencies": {
    "@eluno/core": "github:chuchurex/eluno-core"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2",
    "sass": "^1.97.3"
  },
  "scripts": {
    "build": "npm run sass:build && eluno-build",
    "sass:build": "sass src/scss/main.scss:dist/css/main.css --style=compressed",
    "sass:watch": "sass --watch src/scss/main.scss:dist/css/main.css",
    "dev": "concurrently \"npm run sass:watch\" \"live-server dist --port=3001\"",
    "deploy": "eluno-deploy",
    "build:pdf": "eluno-pdf"
  }
}
